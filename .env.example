# 개발자 포털 > Bot > Reset Token 으로 발급. 절대 커밋 금지
DISCORD_TOKEN=

# 비밀 아님 — 초대 URL 생성용
DISCORD_APP_ID=1528500011476713602
# HTTP 인터랙션 엔드포인트를 쓸 때만 필요. 게이트웨이 방식에선 미사용
DISCORD_PUBLIC_KEY=a51400f60c8778bcb2841ec8419cfb49abb146336902db35e5bd272f25b328c5

# 콘솔이 바라볼 서버. 비우면 자기 자신(로컬 모드 — 봇·DB 를 직접 돌리는 사람)
# 팀원 PC 는 봇이 도는 주소를 넣는다. data/config.json 의 server_url 로도 된다.
SERVER_URL=
