# cp .env.example .env 한 뒤 채운다. .env 는 깃에 올라가지 않는다.

# 디스코드 개발자 포털 > Bot > Token
DISCORD_TOKEN=

# duckdns.org 에서 만든 주소와 토큰
DUCKDNS_SUB=abstraction
DUCKDNS_TOKEN=
SITE_DOMAIN=abstraction.duckdns.org

# 원문 보존 기간(일). 0 이면 무제한. 콘솔에서도 바꿀 수 있다.
# RETENTION_DAYS=90
