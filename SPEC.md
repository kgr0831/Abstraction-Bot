# 디스코드 대화 요약 앱 — 스펙

> 출처: 디스코드 대화 (2026-08-27) 정리
> 참여: 김가람 [JDC], 김민준 [CODE], 유재림 [SSCC], 민윤홍 [AC]

**한 줄 요약:** 디스코드 채팅 로그를 자동 수집해서 날짜·주제별로 묶고 요약해주는 앱. 그중 사업화 가능한 아이디어만 걸러낸다.

## 요구사항

| 출처 | 요구 |
|---|---|
| 유재림 | 대화내역 자동 감지 → 메모리처럼 저장 |
| 유재림 | 저장된 것 중 사업화 가능한 아이디어만 필터링해서 추출 |
| 민윤홍 | 날짜별로 수집 → 주제별로 클러스터링 → 요약 |
| 김가람 | 디코 API 메시지 이벤트로 수집, 오늘 안에 프로토타입 |

## 기술 결정 (대화 중 결론난 것)

1. **MCP 안 씀.** 공식 디코 MCP 없음. 서드파티는 전부 API 래퍼일 뿐 → 에이전트가 API를 직접 다룰 이유가 없으면 그냥 API 직접 래핑하는 게 빠름.
2. **스크래핑 안 함.** 디코가 웹 기반이라 가능은 하지만 ToS 문제. (김민준 지적)
3. **"봇"이 아니라 "앱" — 단, 이 프로젝트엔 봇이 필요.** 디코가 봇 → 앱으로 플로우 전환한 건 맞고 유저 설치형 앱도 생겼지만, 유저 설치형은 인터랙션으로만 동작한다. 게이트웨이 `MESSAGE_CREATE` 도, 채널 히스토리 REST 도 못 쓴다. **자동 수집·날짜별 수집은 길드 설치 봇 + `MESSAGE_CONTENT` 인텐트만 가능.** 관리자 초대는 피할 수 없다.
4. **메시지 읽기는 privacy-first 때문에 귀찮아짐.** Message Content Intent 필요 — 여기가 제일 큰 걸림돌.

## 파이프라인

```
디코 메시지 이벤트 수집 → 저장(날짜별) → 주제 클러스터링 → 요약 → 아이디어 필터
```

> 이 문서는 최초 정리본이다. 확정 계획은 [PLAN.md](PLAN.md) 참고.

## MVP

- discord.py 앱 하나
- `on_message` → SQLite 적재
- `/요약 <날짜>` 슬래시 커맨드 → Claude API로 주제 묶기 + 요약
- `/아이디어 <날짜>` → 사업화 가능한 것만 추출

**skipped:** 주제 클러스터링 알고리즘(임베딩 + KMeans 등). LLM한테 하루치 통째로 던지면 알아서 묶임. 하루 메시지가 컨텍스트를 넘길 때 추가.

## 스코프 밖 (나중에 합칠 수도 있음)

- **논문 읽어주는 봇** — 원래 대화 주제였고 별개 프로젝트. "나중에 합쳐도 될 듯" (유재림)
- 통합 사용량 대시보드
- 프로필 위젯 (김민준이 예전에 만들다 만 것)
