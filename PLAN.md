# 디스코드 대화 수집·요약·아이디어 추출 시스템 — 구현 계획

작성 2026-08-28 · 확정본 · 선행 문서 [SPEC.md](SPEC.md)

---

## 0. 한 줄

디스코드 대화를 봇이 수집해 SQLite에 쌓고, MCP로 에이전트에 노출하고, 에이전트가 요약·아이디어 추출해 노션에 쌓는다. 팀원은 각자 자기 CLI 계정을 브라우저 로그인만으로 등록해서 쓴다.

---

## 진행 상황 (2026-08-28)

| 항목 | 상태 |
|---|---|
| 1단계 수집 봇 | **완료** — 토큰 검증, 커맨드 6개 동기화 확인 |
| 1단계 저장소 | **완료** — `test_db.py` 통과 |
| 2단계 MCP 서버 | **완료** — stdio JSON-RPC 확인, `test_mcp.py` 통과 |
| 2단계 스킬 2개 | **완료** |
| 3단계 계정 등록기 | **완료** — UI 확인, 격리 검증 (`test_launcher.py`) |
| 3단계 디코 OAuth 신원 식별 | **보류** — OAuth 클라이언트 시크릿 미제공 |
| 3단계 봇→런처 작업 전달 | **보류** — §9의 중앙 호스팅 결정에 걸림 |

`MESSAGE CONTENT INTENT` 켜짐 · 접속 확인됨. 1·2단계는 바로 쓸 수 있는 상태다.

봇(`Abstraction Bot#7657`)은 `AI Native 스터디` 서버에 들어가 있다.

**남은 것:** 디스코드에서 `/수집시작` → 요약 품질 확인 → 노션 DB 2개 생성.


## 1. 확정 사항

| # | 결정 | 근거 |
|---|---|---|
| 1 | **길드 설치 봇** + `MESSAGE_CONTENT` 인텐트 | 유저설치 앱은 게이트웨이 메시지 이벤트·히스토리 REST 둘 다 불가. 셀프봇은 ToS 위반 |
| 2 | 개발은 **본인 서버**, 최종은 **기존 서버(관리자 초대)** | 코드 동일, 초대 URL만 재실행 |
| 3 | **Python 3.11 + uv** (discord.py + FastMCP) | 뒷단이 전부 텍스트 처리 |
| 4 | **지정 채널 화이트리스트** + **백필 있음** | 전체 채널은 노출면만 넓힘. 기존 서버 붙이는 순간 과거 데이터가 가치의 전부 |
| 5 | 봇은 **수집기 + 운영 커맨드만** | LLM은 에이전트 쪽. 봇에 LLM 넣으면 프롬프트 고칠 때마다 재배포 |
| 6 | 아이디어 판정은 **Claude Code 스킬** | 코드 아닌 프롬프트 문제. 버전 관리·팀 공유됨 |
| 7 | **고지 + `/수집중단` 옵트아웃 + 화이트리스트** | 코드 20줄. 관리자 승인 설득 카드 |
| 8 | **SQLite** | 표준 라이브러리, 파일 하나 |
| 9 | 봇 구동: **로컬 상시 → 필요시 클라우드** | 백필이 있어 초기 유실 복구 가능 |
| 10 | MCP 클라이언트: **Claude Code** | 스킬이 프로젝트 폴더와 같이 굴러감 |
| 11 | 출력: **노션 DB 2개** (일일 로그 / 아이디어) | 두 요구사항이 서로 다른 출력물 |
| 12 | 중복: **날짜 멱등 덮어쓰기 + 아이디어 유사도 조회 후 병합** | 매일 돌리면 중복이 노션을 쓰레기장으로 만듦 |
| 13 | 멀티유저: **orca 패턴 — 브라우저 로그인만, 자격증명 로컬 보관** | 3단계 |

## 2. 아키텍처

```
                                             ┌──────────────┐
[디스코드 서버] ──게이트웨이 이벤트──▶ [수집 봇] ──▶│  SQLite      │
                                             │ messages.db  │
                                             └──────┬───────┘
                                                    │ 읽기
                                             ┌──────▼───────┐
                                             │  MCP 서버    │
                                             │  (FastMCP)   │
                                             └──────┬───────┘
                                                    │ stdio
                                             ┌──────▼───────┐    ┌─────────┐
                                             │ Claude Code  │───▶│ 노션 MCP │
                                             │  + 스킬 2개   │    └─────────┘
                                             └──────────────┘
```

**설계 원칙: 봇은 멍청하게, 두뇌는 에이전트에.** 봇에는 LLM 호출도 노션 클라이언트도 없다. 노션 연동은 이미 붙어 있는 Notion MCP로 처리하므로 통합 코드가 0줄이다.

## 3. 파일 구조

```
C:\DiscordBot\
├── bot.py                       # 수집 봇
├── mcp_server.py                # MCP 서버
├── db.py                        # 스키마 + 공용 쿼리
├── test_db.py                   # 자체 점검
├── pyproject.toml
├── .env                         # DISCORD_TOKEN (git 제외)
├── data/messages.db
├── SPEC.md · PLAN.md
└── .claude/skills/
    ├── daily-report/SKILL.md    # 날짜별·주제별 정리 → 노션
    └── idea-extract/SKILL.md    # 사업 아이디어 추출 → 노션
```

## 4. 데이터 스키마

```sql
CREATE TABLE messages (
  id           TEXT PRIMARY KEY,   -- 디코 스노플레이크
  guild_id     TEXT NOT NULL,
  channel_id   TEXT NOT NULL,
  channel_name TEXT NOT NULL,
  author_id    TEXT NOT NULL,
  author_name  TEXT NOT NULL,
  content      TEXT NOT NULL,
  created_at   TEXT NOT NULL,      -- ISO8601 UTC
  edited_at    TEXT,
  reply_to_id  TEXT,               -- 맥락 복원용
  thread_id    TEXT,               -- 아이디어 논의가 스레드로 샘
  attachments  TEXT,               -- JSON 배열, URL만
  deleted      INTEGER NOT NULL DEFAULT 0   -- 소프트 삭제, 양보 불가
);
CREATE INDEX idx_msg_ch_time ON messages(channel_id, created_at);

CREATE TABLE channels (            -- 수집 화이트리스트
  channel_id TEXT PRIMARY KEY, guild_id TEXT NOT NULL, added_at TEXT NOT NULL);

CREATE TABLE optouts (             -- 수집 거부자
  user_id TEXT PRIMARY KEY, opted_at TEXT NOT NULL);
```

**버리는 것:** 편집 이력(최신본만), 리액션, 임베드, 다른 봇 메시지, 첨부 파일 실체(URL만).

검색은 `LIKE '%q%'`. `-- ponytail: LIKE 전문검색, 수만 건 넘어 느려지면 FTS5 trigram 토크나이저로 교체 (한국어는 unicode61 말고 trigram)`

## 5. 봇 사양

**인텐트:** `GUILD_MESSAGES`, `MESSAGE_CONTENT` (100서버 미만이면 개발자 포털 토글만, 심사 없음)

**초대 권한 — 최소셋만.** `Administrator` 요청 시 승인 거절됨:
`View Channels` + `Read Message History` + `Send Messages` + `Use Slash Commands`

**이벤트:** `on_message`(적재) / `on_message_edit`(덮어쓰기) / `on_message_delete`(플래그) — 화이트리스트 채널 & 옵트아웃 아닌 사용자만

**커맨드 (운영 전용, 요약 커맨드 없음):**

| 커맨드 | 권한 | 동작 |
|---|---|---|
| `/수집시작 <채널>` | 서버 관리 | 화이트리스트 추가 + 채널에 고지 게시 |
| `/수집중지 <채널>` | 서버 관리 | 화이트리스트 제거 |
| `/backfill <채널> <일수=90>` | 서버 관리 | `channel.history()` 페이징 일회성 적재 |
| `/수집중단` | 본인 | 옵트아웃 등록, 기존 메시지도 삭제 플래그 |
| `/수집재개` | 본인 | 옵트아웃 해제 |
| `/상태` | 누구나 | 수집 채널·건수·마지막 수집 시각 |

## 6. MCP 서버 사양

| 툴 | 시그니처 |
|---|---|
| `list_channels()` | 수집 중인 채널과 건수 |
| `get_conversation(channel, date)` | 하루치 원문 (시간순, reply/thread 관계 포함) |
| `search_messages(query, channel=None, since=None, until=None, limit=200)` | 부분일치 검색 |

전부 삭제 플래그·옵트아웃 사용자 제외. 봇과 별도 프로세스(봇은 상시, MCP는 에이전트가 stdio로 기동)이므로 **SQLite WAL 모드 필수**.

## 7. 노션 출력

**DB 1 — 일일 로그** (민윤홍 요구: 날짜별 수집 → 주제별 묶기 → 정리)

| 속성 | 타입 |
|---|---|
| 날짜 | Date |
| 채널 | Select |
| 주제 | Multi-select |
| 요약 | Text |
| 메시지 수 | Number |
| 원문 링크 | URL |

**DB 2 — 아이디어** (유재림 요구: 사업화 가능한 것만 필터)

| 속성 | 타입 |
|---|---|
| 아이디어 | Title |
| 문제 | Text |
| 대상 사용자 | Text |
| 왜 지금 | Text |
| 상태 | Select — 발굴 / 검토중 / 추진 / 기각 |
| 출처 링크 | URL |
| 발굴일 · 최근 언급 | Date |

**판정은 하드 게이트.** `문제` + `대상 사용자` + `왜 지금` 이 대화에서 **셋 다** 식별될 때만 통과. 하나라도 없으면 탈락. **근거 메시지 링크 첨부 필수** — 환각을 1초에 검증하는 유일한 장치. 이게 없으면 LLM이 잡담 절반을 그럴듯한 사업 아이템으로 포장해 돌려준다.

`상태` 컬럼이 이걸 단순 로그가 아니라 실제 사업 파이프라인으로 만든다.

**쓰기 전 반드시 기존 `아이디어` DB를 조회해 중복 병합** (새 항목 생성 대신 `최근 언급` 갱신).

## 8. 단계

### 1단계 — 수집 (오늘)
봇 + 스키마 + 자체 점검. 본인 서버에서 동작 확인.
산출물: `bot.py`, `db.py`, `test_db.py` · 대략 150줄

### 2단계 — 노출·정리 (오늘)
MCP 서버 + 스킬 2개. Claude Code에 물려서 요약 품질 확인 → 확인 후 노션 DB 생성.
산출물: `mcp_server.py` (~40줄) + 스킬 문서 2개 (**코드 0줄**)

### 3단계 — 멀티유저 CLI 등록 (별도 프로젝트)

orca 패턴 그대로. 자격증명은 본인 PC를 떠나지 않는다.

```
[멤버 PC: 런처 exe 1개]
  ├─ 트레이 아이콘 + 브라우저 UI 1페이지 (터미널 노출 0)
  ├─ "디스코드로 로그인"  → 디코 OAuth → 신원 식별
  ├─ "Claude 계정 연결"   → 격리 홈(CLAUDE_CONFIG_DIR 주입)에서 CLI 로그인 실행
  │                        → 브라우저 팝업, 사용자는 로그인만
  │                        → 자격증명 로컬 저장 (서버 전송 없음)
  └─ 봇 서버로 아웃바운드 연결 유지(WebSocket)

디코에서 `/보고 8-27` → 봇이 해당 멤버 런처로 작업 전달
                     → 로컬 CLI 실행 → 결과를 노션에 기록 → 디코에 링크 회신
```

설치는 exe 하나. 비개발자는 버튼 두 번과 브라우저 로그인이 전부.

> **규모 경고.** 3단계는 1·2단계를 합친 것보다 큽니다. 그리고 1·2단계만으로 팀원은 **노션을 보면 됩니다** — 각자 등록할 실익은 "각자 구독으로 비용 분산"뿐인데, 하루 한 번 요약 비용은 아주 작습니다. 비용이 실제로 아프거나 각자 자기 시점으로 돌리고 싶어질 때 착수하는 것을 권합니다.

## 9. 리스크

| 리스크 | 대응 |
|---|---|
| 관리자가 봇 초대를 거절 | 최소 권한 + 화이트리스트 + 옵트아웃을 승인 근거로 제시. 최악의 경우 새 프로젝트 서버 |
| `MESSAGE_CONTENT` 인텐트 | 100서버 미만이면 토글만. 넘어가면 verification 필요 |
| 3단계 자격증명 위탁 | 로컬 보관 설계로 회피. **서버 보관 방식으로 바꾸지 말 것** — 남의 Claude 계정 토큰을 보관하는 순간 사고 반경이 달라짐 |
| LLM 아이디어 환각 | 3요소 하드 게이트 + 근거 링크 필수 |
| 노션 오염 | 날짜 멱등 + 아이디어 중복 병합 |

**운영 규칙:** 노션 쓰기는 외부 게시다. 자동 실행하지 않는다. 초안을 먼저 보여주고 확인받은 뒤 기록한다. 매일 자동 기록을 원하면 별도로 명시할 것.

## 10. 스코프 밖

- **논문 읽어주는 봇 통합** — 완전히 잊는다. 지금 남기는 확장 포인트는 100% 죽은 코드가 된다. 합칠 때 합친다.
- 통합 사용량 대시보드, 프로필 위젯, 리액션 신호, 첨부 파일 다운로드, 멀티 서버.

## 11. 오늘 할 일

1. 본인 디스코드 서버 생성 (3분)
2. 개발자 포털: 앱 생성 → 봇 유저 추가 → `MESSAGE_CONTENT` 토글 → 토큰 발급
3. 최소 권한 초대 URL로 본인 서버에 초대
4. 1단계 구현 → `/수집시작` → 아무 말이나 쳐서 DB 적재 확인
5. 2단계 구현 → Claude Code에 MCP 물리기 → 요약 품질 확인
6. 품질 OK면 노션 DB 2개 생성 (확인 후)
7. 기존 서버 관리자에게 초대 요청 — 최소 권한·화이트리스트·옵트아웃을 근거로
